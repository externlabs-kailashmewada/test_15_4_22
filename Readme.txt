// run the file by using command node externlabs.js or npm start if your system support the nodemon

// then hit the api that is  http://localhost:3000/generate-pdf
// put the data in json formate like {
					"data":"some text"
					
					}